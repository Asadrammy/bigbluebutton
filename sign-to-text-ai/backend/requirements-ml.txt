# ML and Computer Vision Libraries (Phase 2+)
# Install with: pip install -r requirements-ml.txt

# Core ML/Scientific Computing
numpy>=1.26.4
scipy>=1.11.4

# Computer Vision
opencv-python>=4.10.0.84

# Deep Learning Frameworks
# PyTorch with CUDA support (install CUDA version if GPU available)
# For GPU: pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
# For CPU: pip install torch torchvision
torch>=2.1.1
torchvision>=0.16.1
torchaudio>=2.1.1

# Speech Recognition (STT) - RECOMMENDED: faster-whisper (4x faster)
faster-whisper>=0.10.0
# Alternative: openai-whisper>=20231117

# Video Processing - MediaPipe for hand/pose detection
mediapipe>=0.10.8

# Translation - NLLB (Meta's No Language Left Behind) - RECOMMENDED
transformers>=4.35.2
sentencepiece>=0.1.99
protobuf>=3.20.0

# Text-to-Speech - Coqui TTS (High quality, offline) - RECOMMENDED
# Note: TTS package may not support Python 3.12 yet
# If installation fails, the service will automatically fallback to gTTS (already installed)
# To install TTS manually (if compatible): pip install TTS
# TTS>=0.21.3  # Uncomment when Python 3.12 support is available

# Audio Processing
pydub>=0.25.1
librosa>=0.10.1

# 3D Processing (for avatar)
trimesh>=4.0.0
# pyrender>=0.1.45  # For 3D rendering
# pybullet>=3.2.5  # For physics simulation

# Experiment Tracking (optional but recommended)
# wandb>=0.16.0  # For experiment tracking
# mlflow>=2.9.0  # Alternative for experiment tracking

# Background Jobs
celery>=5.3.4  # For async ML tasks
# rq>=1.15.1  # Alternative to Celery (simpler)

# Audio Processing
pydub>=0.25.1
# webrtcvad>=2.0.10  # Voice activity detection
# librosa>=0.10.1  # Audio analysis

# Utilities
tqdm>=4.66.1  # Progress bars for training
scikit-learn>=1.3.2  # For ML utilities

